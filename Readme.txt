This project is useful for filtering the dataset of CSV format either in ascending or descending order.
The code is written using Python with Flask framework.
The index.html file permitted to take the csv file path and the sort.html file is used to sort the dataframe based on the column which is passed as inputs to perform sorting.
